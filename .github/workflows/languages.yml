# Keeps assets/languages-*.svg in step with what's actually in the repos.
name: refresh language chart

on:
  schedule:
    - cron: "0 4 * * 1"   # Mondays, 04:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python3 scripts/languages.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Commit if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/languages-dark.svg assets/languages-light.svg
          git diff --staged --quiet || git commit -m "chore: refresh language chart"
          git push
